<Page
    x:Class="CPTimetable.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CPTimetable"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ComboBox x:Name="stationBox1" ItemsSource="{Binding}" HorizontalAlignment="Left" Margin="670,206,0,0" VerticalAlignment="Top" Width="401" SelectionChanged="ComboBox_SelectionChanged"/>
        <ComboBox x:Name="stationBox2" ItemsSource="{Binding}" HorizontalAlignment="Left" Margin="670,260,0,0" VerticalAlignment="Top" Width="401" SelectionChanged="ComboBox_SelectionChanged"/>
        <Button Content="Button" HorizontalAlignment="Left" Margin="827,407,0,0" VerticalAlignment="Top" Width="113" Click="Button_Click"/>
        <DatePicker x:Name="date" HorizontalAlignment="Left" Margin="670,313,0,0" VerticalAlignment="Top"/>
        <ComboBox x:Name="hourPicker" ItemsSource="{Binding}" HorizontalAlignment="Left" Margin="1007,313,0,0" VerticalAlignment="Top" Width="62"/>
        <ListView x:Name="trainList" ItemsSource="{Binding}" HorizontalAlignment="Left" Height="279" Margin="10,493,0,-4" VerticalAlignment="Top" Width="1356" SelectionChanged="ListView_SelectionChanged"/>

    </Grid>
</Page>
